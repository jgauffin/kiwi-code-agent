---
spec: 8fa73528
---

# Tasks for User question

- T1 (B2, B3, B4, B13, B16, E1, E2, E3): the question contract in domain terms — request (questions with header, text, options of label plus explanation, single/multi flag), answer per question (chosen labels and free text), plus the pure helpers both the card and the engines use: "is this request answerable", "is every question answered", and the answer text the model receives. [tested]
  - files: src/agent/session/user-question.ts (new), test/user-question.test.ts (new)
  - proves: B2 test/user-question.test.ts a_question_carries_a_header_text_and_options_of_label_and_explanation, B3 test/user-question.test.ts a_single_select_question_takes_one_answer_and_a_multi_select_question_takes_several, B4 test/user-question.test.ts free_text_answers_a_question_whatever_options_it_offered, B13 test/user-question.test.ts nothing_is_answered_on_the_users_behalf_and_an_unanswered_request_says_so, B16 test/user-question.test.ts submit_needs_an_answer_to_every_question_and_says_which_are_missing, E1 test/user-question.test.ts a_multi_select_answer_returns_every_chosen_option_and_the_free_text_together, E2 test/user-question.test.ts free_text_on_a_single_select_question_is_the_answer_and_no_option_is_reported_as_chosen, E3 test/user-question.test.ts a_question_with_no_options_is_free_text_only_and_needs_non_empty_text_to_submit
  - note: spec is silent on Q1/Q2/Q3, so free text is exclusive with a selection on a single-select question (E2), no caps are imposed on question or option counts, and the model is told which part of an answer came from "Other".
- T2 (B6, I2, B15): carry the request and its outcome on the session event stream — a `question_request` and a `question_resolved` event beside the permission pair, a `respondToQuestion` on the session interface, and the status derivation that goes to "needs human" on a request and back to working on its resolution. [tested]
  - files: src/agent/session/code-session.ts, src/agent/session/session-status.ts, src/agent/session/user-question.ts, src/chat/sessions-tree.ts, src/chat/webview/session-tabs.ts, test/session-status.test.ts
  - proves: B6 test/session-status.test.ts a_pending_question_is_its_own_status_apart_from_a_turn_that_merely_ended, B15 test/session-status.test.ts answering_or_cancelling_a_question_puts_the_session_back_to_work
  - note: per F5 a pending question is its own status, `needs_answer`, with its own icon and label in the sessions list and on the tab; the spec names no item I2.
- T3 (B1, B2, B3, B4): the tool itself, once, engine-agnostic: a zod schema over the T1 request, and an `ask` channel on `ToolContext` (today `cwd`, `signal`, `files`) that the tool awaits; the tool returns the answers as its own output text, or the not-answered text. [tested]
  - files: src/agent/openai-session/tools/ask-user.ts (new), src/agent/openai-session/tools/tool.ts, test/tools.test.ts
  - proves: B1 test/tools.test.ts one_tool_of_one_name_and_one_request_shape_serves_every_engine, B2 test/tools.test.ts a_question_carries_a_header_text_options_with_explanations_and_a_single_or_multi_flag, B3 test/tools.test.ts a_question_carries_a_header_text_options_with_explanations_and_a_single_or_multi_flag, B4 test/tools.test.ts the_tool_waits_for_the_card_and_returns_the_chosen_options_and_free_text_as_its_result
  - note: the tool is named `AskUser` and is read-only, so it is never put to a permission prompt; an unanswered outcome is a normal result, not a tool error, so the model reads what happened rather than a failure.
- T4 (B1, B7, B12, B14, B15, E10): own-loop engine — `OpenAiSession` passes an asker into the tool context it builds in `runTool`, emits `question_request`, holds the tool result until `respondToQuestion`, and releases every pending request as unanswered on `interrupt`/`dispose` the way `askPermission` does. [tested]
  - files: src/agent/openai-session/openai-session.ts, test/openai-session.test.ts
  - proves: B1 test/openai-session.test.ts answers_to_every_question_of_one_request_come_back_at_once_as_that_tool_calls_own_result, B7 test/openai-session.test.ts answers_to_every_question_of_one_request_come_back_at_once_as_that_tool_calls_own_result, B12 test/openai-session.test.ts an_interrupted_question_is_resolved_unanswered_and_no_choice_is_invented, B14 test/openai-session.test.ts a_question_from_the_model_becomes_a_request_and_the_session_makes_no_further_progress_until_it_is_resolved, B15 test/openai-session.test.ts a_disposed_session_leaves_no_question_waiting_for_an_answer, E10 test/openai-session.test.ts answers_to_every_question_of_one_request_come_back_at_once_as_that_tool_calls_own_result
  - note: every event now leaves through one `emit`, which drops what a turn still has to say after the session was disposed; before, a turn unwinding past `dispose` pushed onto a closed stream and threw.
- T5 (B1, B7, I2, B15, A10): Claude engine — `SdkSession` passes the same asker into the tool context built in `buildOptions`, emits `question_request`, resolves the waiting tool call from `respondToQuestion`, and releases pending requests as unanswered on dispose. Keep the wait alive past the MCP request timeout if F6 is ruled that way. [tested]
  - files: src/agent/sdk-session/sdk-session.ts, test/sdk-session.test.ts
  - proves: B1 test/sdk-session.test.ts the_same_tool_on_this_engine_asks_through_the_session_and_waits_without_a_deadline, B7 test/sdk-session.test.ts the_same_tool_on_this_engine_asks_through_the_session_and_waits_without_a_deadline, B15 test/sdk-session.test.ts a_question_still_open_when_the_engine_goes_away_ends_unanswered
  - note: per F6 the session imposes no deadline of its own; the engine's own request timeout is not ours to set, and a call it abandons leaves the card answerable, where T6 delivers the answer as the session's next prompt. `tool-server.ts` needed no change: the context the session builds carries the ask channel.
- T6 (B7, B9, I2, I4, E7): route an answer from the webview to the session it belongs to — the protocol message, the provider case beside `permission`, and the manager method that forwards to the live engine and logs the resolution; a request already resolved is answered no further. [tested]
  - files: src/chat/protocol.ts, src/chat/chat-view-provider.ts, src/agent/session/session-manager.ts, src/agent/session/code-session.ts, src/agent/sdk-session/sdk-session.ts, src/agent/openai-session/openai-session.ts, test/session-manager.test.ts
  - proves: B7 test/session-manager.test.ts an_answer_reaches_the_engine_that_asked_and_the_question_and_its_answers_are_logged_in_order, B9 test/session-manager.test.ts an_answer_reaches_the_engine_that_asked_and_the_question_and_its_answers_are_logged_in_order, E6 test/session-manager.test.ts an_answer_given_after_the_engine_let_go_of_the_question_resumes_the_session_as_its_next_prompt, B8 test/session-manager.test.ts a_question_resolved_once_is_answered_no_further
  - note: per F1, E6 is the resumed-session path, so `respondToQuestion` on an engine now reports whether it still held the request and the host delivers the answer as the next prompt when it did not; per F7 an interrupt on the Claude engine releases pending questions as unanswered too.
- T7 (B5, B8, B16, E1, E2, E3, A5, A6, A7): the card — one card per request with one group per question in order, single/multi controls, a free-text "Other" per question, one Submit that is only available when every question has an answer and says what is missing otherwise, read-only with the submitted answers once resolved; the raw tool-call row for the question tool gives way to the card, and a pending card switches the plan session's view to chat the way a permission request does. [tested]
  - files: src/chat/webview/question-card.ts (new), src/chat/webview/chat-transcript.ts, src/chat/webview/events.ts, src/chat/webview/chat-app.ts, src/chat/webview/style.css, test/question-card.test.ts (new), package.json, src/agent/openai-session/tools/ask-user.ts, test/sdk-session.test.ts
  - proves: B5 test/question-card.test.ts one_card_per_request_shows_a_group_per_question_in_the_order_asked_under_one_submit, B8 test/question-card.test.ts a_resolved_card_keeps_what_was_asked_and_answered_and_takes_no_further_input, B16 test/question-card.test.ts submit_needs_an_answer_to_every_question_and_says_which_are_missing, E1 test/question-card.test.ts a_multi_select_answer_submits_every_chosen_option_and_the_free_text_together, E2 test/question-card.test.ts free_text_on_a_single_select_question_is_the_answer_and_no_option_is_reported_as_chosen, E3 test/question-card.test.ts a_question_with_no_options_is_free_text_only_and_needs_non_empty_text_to_submit
  - note: the spec names no items A5/A6/A7; the card needed a DOM to be proven, so jsdom is a new devDependency and the test file names its own environment.
- T8 (B9, I4, E5, E7, E8): replay and lifecycle — `ChatTranscript.reset` rebuilds each card from the logged request and its resolution, read-only and unsubmittable once answered, still pending (and answerable) when it was not, in arrival order, across a tab switch and a reopened view. [tested]
  - files: src/chat/webview/chat-transcript.ts, src/chat/webview/question-card.ts, test/chat-transcript.test.ts (new)
  - proves: B9 test/chat-transcript.test.ts a_replayed_request_shows_its_card_with_the_recorded_answers_read_only_and_unsubmittable, E8 test/chat-transcript.test.ts a_replayed_request_shows_its_card_with_the_recorded_answers_read_only_and_unsubmittable, E5 test/chat-transcript.test.ts a_request_left_open_in_the_log_replays_still_pending_and_still_answerable, E7 test/chat-transcript.test.ts two_requests_replay_as_two_cards_in_arrival_order_each_answered_on_its_own
  - note: the spec names no item I4; a reopened view replays the same log into the same element, proven by a_transcript_shown_again_rebuilds_every_card_from_the_log.
- T9 (B11, A11, Q7): offer the tool where it belongs — add its name to the plan and implement tool sets, leave it out of the mapping run's, and let it through the permission policy unprompted, so the person answers the question instead of first allowing it. Chat sessions get every tool by default; that follows Q7. [tested]
  - files: src/agent/phases/blind-plan.ts, src/agent/phases/implement.ts, src/agent/permissions/permission-policy.ts, src/extension.ts, test/permission-policy.test.ts, test/scope-guard.test.ts, test/implement.test.ts
  - proves: B11 test/permission-policy.test.ts a_session_whose_output_the_user_never_sees_is_not_offered_the_question_tool, B10 test/permission-policy.test.ts a_session_that_needs_a_ruling_asks_through_the_tool_instead_of_stopping_or_blocking
  - note: the spec names no item A11; the unprompted path is proven by asking_the_user_a_question_is_never_put_to_a_permission_prompt_first. Per F3 the plan prompt now asks through the tool instead of "ask, and stop" (the direction checkpoint stands, and Open questions keeps only what is still unanswered); per F2 the implement prompt asks instead of blocking, with ` [blocked: reason]` kept for blockers no answer would remove.

## Verification
- 2026-09-14T21:19:46.561Z: failed, `npm run typecheck && npm test && npm run build` in .
