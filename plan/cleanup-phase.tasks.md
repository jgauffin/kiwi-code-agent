---
spec: 40e5e81c
---

# Tasks for Cleanup phase

- **T1** (B5): count a function's cyclomatic complexity from the branch points in the stripped source — the branching keywords and operators of that file's language, taken from the fixed table `languageOf` knows and matched without a parser — and flag it over `kiwiAgent.cleanup.functionComplexity`, replacing the `functionLines` limit and its setting; nested functions' branches fall to the function they sit in, as units already nest. Split from T2 because the branch set per language is a sitting of its own.
  - files: src/agent/cleanup/unit-size.ts, test/unit-size.test.ts, src/agent/cleanup/oversized.ts, test/oversized.test.ts, src/extension.ts, package.json
  - context: src/agent/cleanup/language.ts, src/agent/cleanup/strip-literals.ts, test/strip-literals.test.ts, src/agent/phases/cleanup.ts
- **T2** (B3, E2, B4, B6, E3, B7): the units measured — the files the feature's implement sessions edited from their run logs (no shell edits), a file that is gone or binary passed over, `kiwiAgent.cleanup.ignore` skipped, a file over `kiwiAgent.cleanup.fileLines` in code lines flagged, a limit of 0 flagging nothing; a file whose language is unknown is measured as a file only, and its comment lines must stop counting as code (the no-language branch of `measureUnits` counts raw non-blank lines today). Retire the type unit with it: the `type` kind, `typeLines` in `Thresholds`, `limitFor`, `anyLimit`, `cleanupThresholds`, the package.json setting and the prompt's limit list.
  - files: src/agent/cleanup/oversized.ts, test/oversized.test.ts, src/agent/cleanup/unit-size.ts, test/unit-size.test.ts, src/extension.ts, package.json, src/agent/phases/cleanup.ts, test/cleanup.test.ts
  - context: src/agent/edits/edited-files.ts, test/edited-files.test.ts, src/agent/cleanup/language.ts, src/agent/cleanup/strip-literals.ts
- **T3** (B1, B2, E1): the trigger — a passing test run measures that feature's edited files, once per feature while the window lives (an in-memory record, so a reload allows it again), and the plan bar says when nothing is over a limit, the case where every limit is off included (`startCleanup` returns before setting a line today). Also the seam every later task tests through: lift the decide-and-report step out of the vscode-bound provider into a module with a test, since src/chat/chat-view-provider.ts has none and cannot have one.
  - files: src/chat/chat-view-provider.ts, src/agent/cleanup/cleanup-run.ts (new), test/cleanup-run.test.ts (new), src/agent/cleanup/oversized.ts
  - context: src/agent/edits/edited-files.ts, src/agent/session/session-manager.ts, src/chat/webview/plan-bar.ts, src/chat/protocol.ts, src/agent/phases/verification.ts
- **T4** (B8, E4, B9, B10, B13): the run itself — started under the feature's latest implement session and continuing its conversation where the engine resumes (fresh where it cannot), no tab and no transcript, the oversized units as its first message, its own prompt naming the limits, writes confined to the flagged files and their folders with no prompt and no shell, listed as `Cleanup: <feature>` in the Sessions view. The report and prompt wording follow T1's measure.
  - files: src/agent/phases/cleanup.ts, test/cleanup.test.ts, src/chat/chat-view-provider.ts, src/extension.ts, src/agent/session/session-manager.ts
  - context: src/agent/phases/scope-guard.ts, test/scope-guard.test.ts, test/session-manager.test.ts, src/chat/sessions-tree.ts, src/agent/phases/reconcile.ts
- **T5** (B11, E5): however the run ends — of its own accord, with an error, or on the user's Stop — measure again and repeat the test run, with one plan-bar line carrying both outcomes and what is still over a limit named and left alone. The second measure covers the flagged files together with the new files the run wrote beside them, under the same ignore globs as the first; today `finishCleanup` measures `child.files` alone with an empty ignore list, and `stop_cleanup` and the error path skip the measure and the tests entirely.
  - files: src/chat/chat-view-provider.ts, src/agent/cleanup/cleanup-run.ts (new), test/cleanup-run.test.ts (new), src/chat/webview/plan-bar.ts
  - context: src/agent/cleanup/oversized.ts, src/agent/edits/edited-files.ts, src/chat/protocol.ts, src/chat/webview/events.ts, src/agent/session/session-manager.ts
- **T6** (B12): a test run that fails after the cleanup goes to the implement session like any other failure, counting against `kiwiAgent.verifyFailureBudget` so an unrepairable split reaches the user instead of looping, and no pass that follows a cleanup starts a second one.
  - files: src/chat/chat-view-provider.ts, test/cleanup-run.test.ts (new)
  - context: src/agent/phases/verification.ts, test/verification.test.ts, src/agent/cleanup/cleanup-run.ts (new)
