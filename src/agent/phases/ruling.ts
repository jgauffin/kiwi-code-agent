/**
 * The one ruling with a fixed spelling, shared by the decisions file, the
 * planner's prompts and the plan view: the rule stands and the code changes.
 * Kept free of node imports so the webview can name it too.
 */
export const KEEP_RULING = 'keep'
